spring.application.name=payment-processing-system
server.port=0

# Use in-memory ActiveMQ for testing
spring.activemq.broker-url=vm://localhost?broker.persistent=false
spring.activemq.in-memory=true

# Test-specific broker URL
pps.broker.service.url=http://localhost:${server.port}

# Camel test configuration
camel.springboot.main-run-controller=true
camel.component.jms.test-connection-on-startup=false
